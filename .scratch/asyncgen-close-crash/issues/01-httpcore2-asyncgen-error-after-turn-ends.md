# httpcore2 async generator error dumps a traceback after a clean turn ends

Category: bug
Status: wontfix

Reported: 2026-08-12

## What happened

A one-shot `-p` run finished cleanly — `finish=stop`, telemetry line
printed, no tool calls — and then a `RuntimeError` traceback was dumped to
the terminal from asyncgen shutdown.

## Repro

Run from `~/.claude` (not the repo):

```
js --model deepseek/deepseek-v4-flash -d -p "Ape. wake up and fart around once or twice and then end turn"
```

Ran against the uv **tool** install (`~/.local/share/uv/tools/js`), not
`.venv`.

## Output

```
  ▸ run model=deepseek-v4-flash  provider=deepseek  base=https://api.deepseek.com  ctx=1000000  max_out=384000  effort=high  vision=off  tools=5
[... normal model output ...]
  ▸ 3675ms  finish=stop  tool_calls=0  139 tok  37.8 tok/s  ttft 2214ms  cache 50%
an error occurred during closing of asynchronous generator <async_generator object PoolByteStream.__aiter__ at 0x7ff6fc21fb50>
asyncgen: <async_generator object PoolByteStream.__aiter__ at 0x7ff6fc21fb50>
Traceback (most recent call last):
  File ".../httpcore2/_async/connection_pool.py", line 427, in __aiter__
    yield chunk
GeneratorExit

During handling of the above exception, another exception occurred:

Traceback (most recent call last):
  File ".../httpcore2/_async/connection_pool.py", line 425, in __aiter__
    async with safe_async_iterate(self._stream) as iterator:
               ~~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^
  File ".../contextlib.py", line 271, in __aexit__
    raise RuntimeError("generator didn't stop after athrow()")
RuntimeError: generator didn't stop after athrow()
```

Session: `20260812T225542067619Z-498e2cad8f084d4d`

## Expected

The run ends after the telemetry line. Nothing else on the terminal.

## Notes

- Not yet established: whether it reproduces every run, only with deepseek,
  only with `-d`, or only from the tool install.
- Nothing is reported as failing — the turn succeeded. This is noise on the
  shutdown path.

## Comments

> *This was generated by AI during triage.*

## Triage Notes

**What we've established so far:**

- **Reproduced, 3/3, deterministic** — every run of the reported command
  against the uv **tool** install prints the traceback after the telemetry
  line.
- **Not reproducible from the repo checkout, 0/3.** The repo env has no
  `httpcore2` at all: `ai==0.2.1` → `httpx 0.28.1` / `httpcore 1.0.9`. The
  tool install runs `ai==0.4.2` → `httpx2 2.10.0` / `httpcore2 2.10.0`.
  The version gap is the discriminator, not the provider. Filed separately
  as `02-ai-sdk-version-drift`.
- **`-d` is irrelevant.** It is `--debug` (stream diagnostics). There is no
  logging configuration anywhere in `js/`, so asyncio's exception handler
  falls through to `logging.lastResort`, which prints to stderr regardless
  of any flag.
- **The provider is irrelevant.** Nothing deepseek-specific is on the path.
- **Mechanism, traced to source.** `httpcore2`'s `PoolByteStream.__aiter__`
  is an async generator that wraps its body iteration in
  `async with safe_async_iterate(...)`. `safe_async_iterate` is an
  `@asynccontextmanager` whose teardown is `finally: await iterator.aclose()`.
  When that generator is abandoned suspended at its `yield` and the event
  loop's `shutdown_asyncgens()` throws `GeneratorExit` into it, the awaiting
  `finally` means the generator does not stop on the `athrow`, so
  `contextlib` raises `RuntimeError("generator didn't stop after athrow()")`.
  **That teardown is not `GeneratorExit`-safe. The defect is upstream in
  httpcore2.**
- **Why the sync path shows it.** The sync wrapper over the async streaming
  call spins a throwaway event loop per call, and `asyncio.run`'s last act is
  `shutdown_asyncgens()`. A shared long-lived loop (the non-blocking runtime)
  does not run that per turn, so the same abandoned generator stays invisible
  until process exit.
- **The originally proposed fix is a no-op.** "Close the stream before
  closing the provider" was based on a misreading: the streaming helper
  already exits its `async with ai.stream(...)` block before the provider is
  closed in the outer `finally`. Ordering in our code is already correct.
  Separately, the generator that leaks is `PoolByteStream.__aiter__`'s, which
  `PoolByteStream.aclose()` does not close — it closes `self._stream`, a
  different object — so no amount of correct closing on our side reaches it.

**What we still need from you (@reporter):**

- Which remedy do we take, given the defect is upstream and the noise is
  cosmetic (the turn itself always succeeds)?
  1. Stop tearing an event loop down per call — make the non-blocking
     shared-loop path the one the `-p` one-shot uses. Structural, already
     the declared direction, largest change.
  2. Hold a reference to the response body iterator and drain-or-`aclose`
     it ourselves before the loop dies. Reaches into SDK internals; brittle
     across `ai` versions.
  3. Install an asyncio exception handler that drops this one message.
     Cosmetic, and it would mask genuine teardown errors.
  4. Report upstream to httpcore2 and carry nothing locally — accept the
     noise until it is fixed there.
- Is cosmetic-only enough to justify (1) now, or does (1) wait for the
  non-blocking migration to land on its own schedule?

---

> *This was generated by AI during triage.*

## Agent Brief

**Category:** bug
**Summary:** End a completed one-shot model stream without an asynchronous-generator
shutdown traceback.

**Current behavior:**
A successful one-shot prompt on the newer AI SDK stack prints its response and
telemetry, exits with status zero, and then emits a `RuntimeError` while the event
loop closes an abandoned `httpcore2` response-body iterator. The same prompt on
the older locked SDK stack exits cleanly. The failure is deterministic on the
newer stack and does not mean the model request failed.

**Desired behavior:**
A completed model stream must release every response-body iterator before its
event loop shuts down. One-shot and long-lived runtimes should both finish
without post-turn transport noise, while genuine asynchronous teardown failures
remain visible.

**Key interfaces:**
- `stream_model_async()` and its underlying streaming context — completion must
  include ownership and closure of every iterator created while consuming the
  response
- The synchronous one-shot boundary — event-loop shutdown must not discover an
  iterator that should have been finalized by the stream owner
- Provider transport cleanup — the solution must work with the supported AI SDK
  baseline and must not depend on DeepSeek-specific behavior

**Acceptance criteria:**
- [ ] The reported one-shot command on the supported newer SDK stack exits zero
      after telemetry with no asynchronous-generator traceback
- [ ] A deterministic regression test exercises stream finalization followed by
      event-loop shutdown
- [ ] Normal response text, usage metadata, telemetry, and exit status are
      unchanged
- [ ] Unrelated asynchronous-generator and event-loop exceptions are not
      globally suppressed
- [ ] Long-lived/nonblocking turns continue to reuse their loop normally

**Out of scope:**
- Choosing and aligning the repository's AI SDK version; that is tracked by the
  sibling SDK-drift issue and should land first
- Rewriting the runtime around a different concurrency model
- Hiding all asyncio shutdown diagnostics with a broad exception handler

---

> *This was generated by AI during triage.*

## Triage Outcome

Closed as already implemented by commit `d40d8cb` on `main`.

The supported `ai==0.4.2` boundary now pins `openai==2.44.0` and
`httpx==0.28.1`, preventing the editable tool install from floating to
OpenAI 3 and its `httpx2`/`httpcore2` transport. The installed tool environment
contains neither `httpx2` nor `httpcore2`. A regression test also verifies that
the synchronous stream path finalizes its generator before `asyncio.run()`
shuts down the event loop; no global exception suppression was added.

After `just install`, the exact reported command was rerun from `~/.claude`.
It returned a normal response, printed telemetry, exited zero, and emitted no
asynchronous-generator traceback.
