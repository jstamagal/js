Move a completed inbox unit into the vault archive.

Usage:
- Call only after the relevant pages are written and any needed log entry exists.
- `unit` is the top-level inbox file or folder name.
- The tool refuses already-archived or missing units to avoid false completion.
