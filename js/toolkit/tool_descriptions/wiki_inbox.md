List unprocessed source units in a wiki vault inbox.

Usage:
- A top-level file is one source unit; a top-level folder is one project unit.
- Process one unit per ingest cycle unless the user directs otherwise.
- Use the returned names as `unit` values for archive or finish operations.
