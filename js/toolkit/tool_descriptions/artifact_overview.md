Return the artifact library manifest and curation overview.

Usage:
- Call first when working in artifact mode.
- The result includes root path, base URL, counts, unassigned slugs, topics, and recent artifacts.
- Use it to decide whether to search, read, curate, or create.
