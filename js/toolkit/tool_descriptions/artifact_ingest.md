Bulk ingest local files into the artifact library.

Usage:
- `paths` may be newline- or whitespace-separated.
- Paths are resolved relative to the current tool context.
- Optional `tags` and `desc` are applied to the ingested artifacts.
- The artifact CLI handles copying, rendering, indexing, and sync.
