Return a wiki vault's domain lens and current backlog.

Usage:
- Call first in wiki work so page choices match the vault's PURPOSE.md.
- `vault` accepts `creative`, `general`, or an explicit path.
- The result includes page counts and inbox status for planning the next unit.
