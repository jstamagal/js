Append a structured entry to a vault `log.md`.

Usage:
- `op` is one of ingest, synth, lint, or skip.
- `title` is the visible log heading; `note` is optional detail.
- For normal ingest close-out, prefer `wiki_finish_ingest` so archive and log stay atomic.
