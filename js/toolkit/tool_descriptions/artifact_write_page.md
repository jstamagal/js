Create or update a markdown-backed artifact page.

Usage:
- Omit `slug` to create a new page; pass an existing slug to update that page.
- `body` is markdown rendered into the shared artifact style.
- `tags` are comma-separated; `desc` is a short index description.
- Use this for reference docs and reports that belong in the artifact library.
