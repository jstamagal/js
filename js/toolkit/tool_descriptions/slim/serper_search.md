Google search via Serper. Keyword queries: exact phrases, names, error messages,
recent news. Returns Google's organic results as title, URL, and snippet.
{{#if tavily_search}}
`tavily_search` returns extracted page content and a synthesized answer instead
of snippets.
{{/if}}
{{#if exa_search}}
`exa_search` finds things you can describe but not name.
{{/if}}
{{#if docs_search}}
`docs_search` is for library documentation.
{{/if}}
