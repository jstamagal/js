Read one artifact's metadata and available text preview.

Usage:
- `slug` may be an exact slug, HTML path, title fragment, or slug fragment.
- Ambiguous matches return an error listing candidate slugs.
- Text previews are capped by the runtime; binary assets return metadata only.
