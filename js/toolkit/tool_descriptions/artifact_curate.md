Install artifact curation data through the artifact CLI.

Usage:
- `curation_json` must be complete valid JSON with topics, assignments, and refs.
- Validate identity and slug choices before replacing curation state.
- The artifact CLI performs the actual write and any configured sync.
