Search the artifact library.

Usage:
- Searches title, tags, description, type, original filename, and text-like contents.
- Use focused terms; read a chosen result with `artifact_read` before updating it.
- `limit` bounds returned matches.
